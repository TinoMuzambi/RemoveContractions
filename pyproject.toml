[project]
name = "remove-contractions"
version = "2.0.0"
description = "Expand common English contractions while preserving prose formatting."
requires-python = ">=3.12"
dependencies = [
  "Flask==3.1.3",
  "gunicorn==23.0.0",
  "WTForms==3.2.1",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
